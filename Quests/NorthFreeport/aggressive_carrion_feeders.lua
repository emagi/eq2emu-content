--[[
	Script Name		:	aggressive_carrion_feeders.lua
	Script Purpose	:	Handles the quest, "Aggressive Carrion Feeders"
	Script Author	:	Xiki
	Script Date		:	3/15/2025
	Script Notes	:	Auto generated with QuestParser.

	Zone			:	North Freeport
	Quest Giver		:	Neophyte Jhanov
	Preceded by		:	None
	Followed by		:	None
--]]


function Init(Quest)
	SetQuestFeatherColor(Quest, 3)
	SetQuestRepeatable(Quest)
	AddQuestStepKill(Quest, 1, "I need to kill giant vulriches within the Commonlands.", 12, 100, "I need to destroy several giant vulriches found within the commonlands.", 611, 330087)
	AddQuestStepCompleteAction(Quest, 1, "QuestComplete")
end

function Accepted(Quest, QuestGiver, Player)
	-- Add dialog here for when the quest is accepted
end

function Declined(Quest, QuestGiver, Player)
	-- Add dialog here for when the quest is declined
end

function Deleted(Quest, QuestGiver, Player)
	-- Remove any quest specific items here when the quest is deleted
end

function QuestComplete(Quest, QuestGiver, Player)
	-- The following UpdateQuestStepDescription and UpdateTaskGroupDescription are not needed, parser adds them for completion in case stuff needs to be moved around
	UpdateQuestStepDescription(Quest, 1, "I have killed giant vulriches within the Commonlands.")
	UpdateQuestTaskGroupDescription(Quest, 1, "I have slain several giant vulriches within the Commonlands.")

	UpdateQuestDescription(Quest, "The number of giant vulriches within the Commonlands has been reduced thanks to my selective hunting.")
	GiveQuestReward(Quest, Player)
end

function Reload(Quest, QuestGiver, Player, Step)
	if Step == 1 then
		QuestComplete(Quest, QuestGiver, Player)
	end
end
